# Documentación de desarrollo (en construcción)

Documentación técnica **del código de Sigil**: cómo está montado, estructura de carpetas, decisiones
de implementación, y cómo extenderlo. Se irá completando a medida que se estabilice la estructura
del repositorio.

Para el uso, operación, auditoría y la guía general del desarrollador, ver
**[`docs/guias/`](../guias/00-indice.md)**.

## Pendiente

- [ ] Estructura del código backend (`src/backend/`) y el flujo de sellado.
- [ ] Estructura del Code App (`src/frontend/sigil-app/`) y su modelo de datos/servicios generados.
- [ ] Herramienta de despliegue (`tools/Sigil.Deploy/`).
- [ ] ALM: solución, Releases, y el pipeline de metadata (`solutions/`).
