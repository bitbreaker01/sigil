/// <reference types="vite/client" />
// Brings in Vite's ambient module declarations (e.g. `*?url` asset imports used for the
// pdf.js worker, plan A) without polluting the app's global type surface.
